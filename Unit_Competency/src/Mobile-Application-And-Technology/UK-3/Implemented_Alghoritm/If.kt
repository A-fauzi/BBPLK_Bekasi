package `Mobile-Application-And-Technology`.`UK-3`.Implemented_Alghoritm

fun main() {
    val kondisi = true
    if (kondisi) {
        // Blok kode yang akan di eksekusi disini
    }


    // Contoh
    val nilai = 90
    if (nilai >= 80) {
        println("Nilai kamu $nilai, Selamat ya!")
    }

    // Apa bila hanya terdapat satu baris kode, blok kode yang ada didalam if boleh tidak dikurung
    if (nilai == 90) println("Nilai kamu $nilai, Selamat ya!")
}

