package `Mobile-Application-And-Technology`.`UK-3`.Implemented_Alghoritm

fun main() {
    val kondisi = true
    if (kondisi) {
        // Blok kode yang akan dieksekusi
        // Jika kondisi bernilai true
    } else {
        // Blok kode yang akan diekseskusi
        // Jika kondisi bernilai false
    }

    val password = 12345
    if (password == 12345) {
        println("Password benar")
    } else {
        println("Password salah")
    }
}
