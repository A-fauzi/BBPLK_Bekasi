package `Mobile-Application-And-Technology`.`UK-3`.Implemented_Alghoritm

fun main() {
    val kondisi = true

    if (kondisi){
        // Blok kode yang akan dieksekusi
        // Jika kondisi pertama bernilai true
    } else if (kondisi){
        // Blok kode yang akan dieksekusi
        // Jika kondisi kedua bernilai true
    } else if (kondisi){
        // Blok kode yang akan dieksekusi
        // Jika kondisi ketiga bernilai true
    } else {
        // Blok kode yang akan dieksekusi
        // Jika semua kondisi bernilai false
    }

    val usia = 16
    if (usia >= 18){
        println("Anda boleh menikah")
    } else if (usia == 17) {
        println("Buat ktp dulu")
    } else {
        println("Anda belum siap nikah")
    }
 }
